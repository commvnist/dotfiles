# starship

GNU Stow package for the Starship prompt configuration.

## Dependencies

```sh
sudo pacman -S --needed starship
```

No AUR package is required.

## Install

From the repository root:

```sh
stow starship
```

This links `starship/.config/starship.toml` to
`~/.config/starship.toml`.
