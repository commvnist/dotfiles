# vim

GNU Stow package for Vim configuration.

## Dependencies

```sh
sudo pacman -S --needed vim
```

No AUR package is required.

## Install

From the repository root:

```sh
stow vim
```

This links `vim/.vimrc` to `~/.vimrc`.
