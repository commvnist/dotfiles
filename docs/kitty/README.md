# kitty

GNU Stow package for Kitty terminal configuration.

## Dependencies

```sh
sudo pacman -S --needed kitty
```

No AUR package is required.

## Install

From the repository root:

```sh
stow kitty
```

This links `kitty/.config/kitty/kitty.conf` to
`~/.config/kitty/kitty.conf`.
